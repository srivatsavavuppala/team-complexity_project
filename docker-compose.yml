version: '3.8'

services:
  ai-recruiter:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - JWT_SECRET=${JWT_SECRET:-ai_recruitment_helper_jwt_secret_2024}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - DB_PATH=/app/data/database.sqlite
    volumes:
      - ai-recruiter-data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5000/api/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  ai-recruiter-data: